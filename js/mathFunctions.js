function mathRand() {
  return Math.random();
}
function mathRandInt(limit) {
  return Math.floor(mathRand() * limit);
}

function sin(angle) {
  return Math.sin(angle);
}

function cos(angle) {
  return Math.cos(angle);
}
